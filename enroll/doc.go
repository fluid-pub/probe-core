// Package enroll implements POST /api/v1/enrollment/enroll for probes (and mirrored workloads).
//
// This tree is kept in sync with fluid/agents/core/enroll (same API surface).
// When changing enrollment behavior, update both packages in the same change.
package enroll
